package main

/*
import (
	"context"
	"fmt"

	"github.com/phanikumarps/sap/odata"
	"github.com/phanikumarps/sap/odata/isu/umc"
)

func main() {
	d := odata.SetDefaults()
	s := umc.NewService(d.HostUrl, d.AuthToken)
	ctx := context.TODO()
	acct, err := s.GetAccount(ctx, "1234")
	if err != nil {
		fmt.Println(err)
	}
	fmt.Println(acct)
}
*/
// Accounts('%s')/ContractAccounts

// Accounts('%s')/ContractAccounts('%s')/AccountAddress

//
